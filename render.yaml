services:
  - type: web
    name: github-dashboard-backend
    env: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: GITHUB_TOKEN
        sync: false
      - key: PORT
        value: 5000
      - key: NODE_ENV
        value: production
