{
  "name": "github-developer-intelligence-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:backend": "cd backend && cross-env PORT=5001 npm run dev",
    "dev:frontend": "cd frontend && cross-env VITE_API_URL=http://localhost:5001 VITE_PORT=5174 npm run dev",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "test:backend": "cd backend && npm test",
    "test:frontend": "cd frontend && npm test",
    "test": "npm run test:backend && npm run test:frontend",
    "install:all": "cd backend && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^10.1.0"
  }
}
